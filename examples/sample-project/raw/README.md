# raw/

Immutable source material. Neither the AI agent nor the `llmw` CLI may modify files in this directory. Drop new material into `inbox/`; converted output (e.g. from PDFs) goes into `processed/`.
