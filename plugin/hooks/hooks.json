{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v llmw >/dev/null 2>&1 || { command -v uv >/dev/null 2>&1 && uv tool install -q \"git+https://github.com/duddudcns/llm-wiki-cli.git\" || pip install -q --user \"git+https://github.com/duddudcns/llm-wiki-cli.git\"; }"
          }
        ]
      }
    ]
  }
}
